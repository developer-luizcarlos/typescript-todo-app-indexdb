export const btnAddTask = document.querySelector(
  ".btn--add-task"
) as HTMLButtonElement;
export const inputAddTask = document.querySelector(
  ".add-task"
) as HTMLInputElement;
export const listTasks = document.querySelector(".list") as HTMLUListElement;
export const timeCurrentDate = document.querySelector(
  ".current-date"
) as HTMLTimeElement;
